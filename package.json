{
  "name": "f15-internal-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "functions"
  ],
  "scripts": {
    "build:frontend": "npm --prefix frontend run build",
    "deploy": "npm run build:frontend && firebase deploy",
    "emulators": "firebase emulators:start"
  }
}