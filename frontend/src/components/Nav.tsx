import { Link, useLocation } from 'react-router-dom'
import { useAuth } from '../hooks/useAuth'
export default function Nav(){ const { pathname } = useLocation(); const { logout } = useAuth(); const Item=({to,label}:{to:string;label:string})=> (<Link to={to} className={`px-3 py-2 rounded-lg border border-slate-800/80 hover:bg-slate-800/60 ${pathname===to?'bg-slate-800 text-white':'text-slate-300'}`}>{label}</Link>);
  return (<nav className='sticky top-0 z-20 backdrop-blur bg-slate-950/70 border-b border-slate-800/60'><div className='max-w-6xl mx-auto px-4 py-3 flex items-center gap-2'><div className='font-semibold text-slate-100 mr-2'>Frame 15</div><Item to='/' label='Dashboard'/><Item to='/projects' label='Projects'/><Item to='/inventory' label='Inventory'/><Item to='/documents' label='Documents'/><Item to='/ai' label='AI Marketing'/><div className='ml-auto'><button className='px-3 py-2 rounded-lg border border-slate-800 text-slate-300 hover:bg-slate-800/60' onClick={logout}>Log out</button></div></div></nav>) }